<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All matches
    </h1>

    <a href="/matches/new" class="btn btn-block btn-outline-secondary">
      Add a new match
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "match_templates/matches_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Date
        </th>

        <th>
          Start
        </th>

        <th>
          End
        </th>

        <th>
          Sender availability
        </th>

        <th>
          Recipient availability
        </th>

        <th>
          State
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @matches.each do |match| %>
      <tr>
        <!-- Display match.id start -->
        <td>
          <%= match.id %>
        </td>
        <!-- Display match.id end -->

        <!-- Display match.date start -->
        <td>
          <%= match.date %>
        </td>
        <!-- Display match.date end -->

        <!-- Display match.start start -->
        <td>
          <%= match.start %>
        </td>
        <!-- Display match.start end -->

        <!-- Display match.end start -->
        <td>
          <%= match.end %>
        </td>
        <!-- Display match.end end -->

        <!-- Display match.sender_availability_id start -->
        <td>
          <% if match.sender_availability.present? %>
            <a href="/slots/<%= match.sender_availability.id %>">
              <%= match.sender_availability.user_id %>
            </a>
          <% end %>
        </td>
        <!-- Display match.sender_availability_id end -->

        <!-- Display match.recipient_availability_id start -->
        <td>
          <% if match.recipient_availability.present? %>
            <a href="/slots/<%= match.recipient_availability.id %>">
              <%= match.recipient_availability.user_id %>
            </a>
          <% end %>
        </td>
        <!-- Display match.recipient_availability_id end -->

        <!-- Display match.state_id start -->
        <td>
          <%= match.state_id %>
        </td>
        <!-- Display match.state_id end -->

        <!-- Display match.created_at start -->
        <td>
          <%= time_ago_in_words(match.created_at) %> ago
        </td>
        <!-- Display match.created_at end -->

        <!-- Display match.updated_at start -->
        <td>
          <%= time_ago_in_words(match.updated_at) %> ago
        </td>
        <!-- Display match.updated_at end -->

        <td>
          <a href="/matches/<%= match.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @matches, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
